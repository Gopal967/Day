#include <stdio.h>
#include <math.h>

int main() {
    double x, y, result;
    printf("Enter x value: ");
    scanf("%lf", &x);
    printf("Enter y value: ");
    scanf("%lf", &y);
    result = pow(x, y);
    printf("Your Answer: %.2f\n", result);
    return 0;
}
